version: '3.8'

services:
  flask_web:
    build: .
    container_name: my_flask_app
    env_file:
      - .env
    ports:
      - "5000:5000"
    restart: unless-stopped
    command: gunicorn -c gunicorn.conf.py wsgi:app